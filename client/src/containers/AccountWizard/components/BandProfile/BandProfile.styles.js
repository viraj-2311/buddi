import styled from 'styled-components';
import WithDirection from '@iso/lib/helpers/rtl';

const BandProfileStyleWrapper = styled.div`
  .actionBtnWrapper {
    text-align: center;
    margin-top: 30px;
  }
`;

export default WithDirection(BandProfileStyleWrapper);
