import styled from 'styled-components';
import { palette } from 'styled-theme';

const RequestClarificationWrapper = styled.div`
  width: 100%;
  .actions {
    text-align: right;
    margin-top: 20px;
    
    button {
      margin-left: 10px;
    }
  }

`;

export default RequestClarificationWrapper;
