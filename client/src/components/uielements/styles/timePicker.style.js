import styled from 'styled-components';
import { palette } from 'styled-theme';

const AntTimePicker = (ComponentName) => styled(ComponentName)`
  &.ant-picker {
    height: 35px;
  }
`;

export default AntTimePicker;