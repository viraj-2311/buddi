import React from "react";

export const useIsMobile = () => {
  const defaultWidth = window && window?.innerWidth ? window?.innerWidth : 1080;
  const defaultHeight =
    window && window?.innerHeight ? window?.innerHeight : 900;

	const [width]
};
